# AI 教學文章撰寫規範

## 適用範圍

- 本規範適用於專案內所有新增的 Markdown，以及所有被修改且內容涉及 AI 架構、模型或演算法的既有 Markdown。
- 修改既有文章時，必須同步將該文章調整為符合本規範；不要求在未修改文章時一次重寫全站既有內容。

## 教學敘事與說明方式

- 每篇文章必須先交代技術出現的故事背景，以及它要解決的實際痛點，讓讀者先理解「為什麼需要它」，再說明「它如何運作」。
- 全文必須使用淺顯易懂、生活化的語言與例子。專有名詞首次出現時，必須保留其英文名稱，並用白話解釋其意義，不可只寫中文譯名、堆疊術語或公式。
- 若本站已有介紹該專有名詞的文章，每篇文章第一次出現該名詞時，必須使用相對 Markdown 連結連至該文章；同一篇文章後續再次出現時不必重複連結。若本站沒有對應文章，仍須保留英文名稱與首次出現時的白話解釋，但不必建立空連結。
- 必須選擇一個具體、容易理解且數值可追蹤的輸入範例，將輸入一步一步帶入模型、架構或演算法。
- 必須依實際資料流順序，完整呈現每個運算階段、資料轉換、中間結果與最終輸出，並說明各階段如何回應前述痛點。
- 涉及演算法、架構或模型的文章必須包含 SVG 架構圖。架構圖必須依實際資料流為流程節點編號，並呈現完整資料流、主要運算階段，以及輸入、中間結果與輸出的流向。
- SVG 必須放在對應的 `docs/.../assets/<主題>/` 目錄，文章須以相對路徑嵌入，並提供能清楚描述圖意的替代文字。
- 可依文章內容自行判斷是否額外加入網路上的架構圖作為補充；若加入，必須標示圖片來源與連結，並確認其授權允許使用。網路圖片不得取代前述必要的 SVG 架構圖。

## 逐步運算與矩陣規則

- 正文逐步運算必須使用與 SVG 架構圖相同的流程編號、階段名稱與先後順序，依架構圖逐步展示運算。
- 架構圖中的每個主要運算階段，都必須在正文展示對應的輸入、資料轉換、中間結果與輸出；不得出現架構圖有節點但正文跳過，或正文新增未在架構圖呈現的主要階段。
- 矩陣乘法只需要呈現「矩陣 A · 矩陣 B = 矩陣 C」的關係、實際矩陣及結果矩陣，不需要展開每一行與每一列的逐項乘加計算。
- 每一個輸入矩陣、權重矩陣、中間結果矩陣與輸出矩陣，都必須清楚標示名稱與 `shape`。
- 所有矩陣都必須呈現數值：
  - 小型矩陣必須完整列出每一個數值，不可用省略號代替。
  - 若矩陣大到不適合完整展示，例如 `512 × 512`，可以只列出上、下、左、右邊界的代表數值，矩陣中間以 `...` 省略，但仍須標示完整 `shape`。
  - 大型矩陣是否省略以文章可讀性判斷，不設定固定尺寸門檻；不得以「矩陣太大」為由完全略過矩陣或只寫 shape。
- 除了矩陣乘法的逐行逐列乘加細節，以及大型矩陣允許省略的中間區域數值之外，不得省略任何推導步驟、資料轉換、中間結果或必要說明。

## 完稿檢查

完成或修改相關 Markdown 前，逐項確認：

- 已包含故事背景與實際痛點。
- 已使用白話、生活化的方式解說。
- 已保留所有專有名詞的英文名稱，且首次出現時有白話解釋。
- 本站已有介紹文章的專有名詞，已在每篇文章首次出現時加入相對站內連結。
- 已提供 SVG 架構圖，且圖中包含完整、編號清楚的資料流。
- SVG 已放在對應的 `docs/.../assets/<主題>/` 目錄、引用路徑有效，且具有能描述圖意的替代文字。
- 若有加入網路上的架構圖，已標示圖片來源與連結，並確認其授權允許使用。
- 架構圖與逐步運算的編號、名稱及先後順序一致。
- 架構圖中的每個主要階段，均有對應的輸入、資料轉換、中間結果與輸出。
- 已用具體輸入完成逐步運算，且運算順序完整。
- 每個矩陣均有名稱、`shape` 與數值。
- 小型矩陣沒有省略數值；大型矩陣只省略中間區域，且保留邊界代表數值。
- 矩陣乘法沒有展開逐行逐列的乘加計算。
- 除上述允許省略的內容外，沒有跳過推導步驟、資料轉換或中間結果。
