# pip fallback for Read the Docs or environments that cannot use uv.
# Local development and CI should prefer:
#   uv sync --locked

mkdocs[i18n]>=1.6,<2
mkdocs-material>=9,<10
pymdown-extensions>=10,<11
